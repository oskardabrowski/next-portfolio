/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	experimental: {
		styledComponents: true,
	},
};

module.exports = nextConfig;

// images: {
// 	loader: "akamai",
// 	path: "",
// },
